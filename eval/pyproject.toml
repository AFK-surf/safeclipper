[project]
name = "safecliper-eval"
version = "0.1.0"
description = "Evaluation scripts for safecliper screenshot redaction benchmarks."
requires-python = ">=3.11"
dependencies = [
  "datasets>=4.4.1",
  "openai>=2.0.0",
  "pillow>=12.0.0",
  "python-dotenv>=1.2.0",
]

[tool.uv]
package = false
